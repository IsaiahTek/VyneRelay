{
  "name": "vynrelay-monorepo",
  "version": "1.0.0",
  "description": "VynRelay: A production-grade real-time messaging ecosystem with Shared Port Architecture and resilient WebSocket/SSE fallback.",
  "keywords": ["realtime", "messaging", "monorepo", "vynrelay", "websocket", "sse", "nestjs", "flutter"],
  "author": "Vynelix",
  "private": true,
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm test --workspaces --if-present"
  },
  "workspaces": [
    "server",
    "sdk/*",
    "examples",
    "examples/*",
    "samples/*"
  ],
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
